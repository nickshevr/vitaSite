---
import BaseHead from '../components/BaseHead.astro';
import Block from '../components/Block.astro';
import MainBlock from '../components/MainBlock.astro';
import InfoBlock from '../components/InfoBlock.astro';
import Footer from '../components/Footer.astro';
import ProductCard from '../components/ProductCard.astro';
import BuyNow from '../components/BuyNow.astro';

import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';

---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<main class="main">
			<MainBlock>
				<h2 class="title titleContent">Волшебной таблетки не&nbsp;было,&nbspно&nbsp;мы&nbsp;её&nbsp;сделали</h2>
				<p class="subtitleContent text">Превратили витамины, минералы и&nbsp;аминокислоты во&nbsp;вкусныеи работающие напитки, которые укрепляют здоровье и&nbsp;дают ощутимый заряд энергии. А&nbsp;еще тут мармеладные медведи для твоих бадди.</p>
			</MainBlock>
			<InfoBlock />
			{false && 			<div style="margin-top: 3em;" />
			<Block imgUrl='/main_photo_2.png'>
				<h2 class="revertedTitle">
					<span>Не обязательно начинать новую жизнь</span>
					<br /><span>— можно постепенно улучшать</span>
					<br /><span>существующую!</span>
				</h2>
			</Block>
			<div style="margin-top: 3em;" class="productContent">
				<div class="cards">
					<ProductCard
						imgUrl='/productImage.png'
						url='product/tovar1'
						title='Multi Complex'
						text='Витаминно-минеральный комплекс для ежедневных потребностей организма'
						bullets={['капсулы', 'витамины']}
					/>
					<ProductCard
						imgUrl='/productImage.png'
						url='product/tovar2'
						title='Multi Complex 2'
						text='Витаминно-минеральный комплекс для новых потребностей организма'
						bullets={['бадик', 'в садик']}
					/>
				</div>
				<BuyNow />
			</div>}
		</main>
		<Footer />
	</body>
	<style>
		.main {
			width: 100;
		}

		@media (min-width: 720px) {
			.main {
				max-width: 1400px;
			}
		}

		.titleContent {
			padding-top: 290px;
			padding-left: 29px;
			padding-right: 29px;
		}

		.subtitleContent {
			margin-top: 20px;
			padding: 0 14px;
		}

		@media (min-width: 720px) {
			.titleContent {
				padding-top: 440px;
			}
			.subtitleContent {
				margin-top: 20px;
			}
		}

		.cards {
			display: flex;
			justify-content: space-between;
		}
		.productContent {
			margin-top: 1em;
			padding: 0 1em;
		}

		.title {
			font-weight: 400;
			font-size: 34px;
			line-height: 94%;
			letter-spacing: -0.04em;
			text-align: center;
			color: #f7f3df;
		}

		.text {
			font-weight: 500;
			font-size: 15px;
			line-height: 100%;
			text-align: center;
			color: #f7f3df;
		}

		@media (min-width: 720px) {
			.title {
				font-weight: 400;
				font-size: 64px;
				line-height: 87%;
				letter-spacing: -0.04em;
			}

			.revertedTitle {
				padding-top: 530px;
				color: #3f1a05;
				text-align: center;
			}
			.revertedTitle span {
				padding: 0 10px;
				background: #ffbaf1;
			}

			.text {
				font-size: 20px;
				line-height: 105%;
			}
		}
	</style>
</html>
